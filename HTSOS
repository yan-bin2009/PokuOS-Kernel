HTSOS：一种等级感知的全栈贯穿操作系统调度框架
摘要

本文提出并实现了一种名为 HTSOS（Hierarchical Tiered Scheduling Operating System）的调度框架。该框架将进程按关键性划分为四个等级，并使等级标记贯穿调度、内存管理、文件系统、I/O 及安全沙盒等所有子系统。与传统的优先级调度不同，HTSOS 将等级作为系统资源分配的第一级决策依据，旨在保证内核自身在任何负载条件下不被驱逐，关键系统进程在资源压力下优先存活。本文详细阐述了 HTSOS 的等级体系、调度算法、内存管理策略、I/O 队列分级、沙盒机制、锁与优先级继承以及自愈机制，并给出了完整的实现状态与性能模型。
1 引言

通用操作系统调度器长期以来追求的核心目标是公平性，即所有进程按权重平等地获得 CPU 时间。然而，公平性并非在所有场景下都是最优的。系统关键进程（如 init、shell、watchdog）的存活优先级应当高于用户编译任务，内存耗尽时系统应当优先终止用户进程而非内核自身。

基于上述观察，HTSOS 在系统中建立了明确的优先级契约。该契约以等级（tier）为载体，贯穿整个软件栈。等级不是调度器内部的一个数值，而是系统各子系统共同遵守的语义约定。

HTSOS 的设计遵循三项核心原则：其一，内核永不宕机，KERNEL 等级进程在任何负载条件下不被驱逐；其二，关键任务有保障，SYSTEM 等级进程在资源压力下优先存活；其三，普通任务公平，USER 等级进程在资源充足时公平轮转；其四，系统能自愈，关键进程崩溃后由看门狗机制自动重启。
2 等级体系

HTSOS 定义四个等级，每个等级具有明确的调度优先级和内存驱逐策略。

KERNEL 等级为内核自身，调度优先级最高，可硬抢占任何低等级任务，内存永不驱逐，负责运行内核线程和中断处理。

SYSTEM 等级为系统关键进程，调度优先级高，可抢占 USER 等级任务，内存驱逐优先级极低。典型进程包括 init、shell 和 watchdog。

USER 等级为普通用户进程，调度优先级正常，内存可被驱逐，运行用户程序和第三方程序。

CRITICAL 等级为 USER 等级的临时提升状态。当系统检测到内存压力时，当前正在运行的 USER 进程被自动提升至此等级，其调度优先级和内存驱逐策略与 SYSTEM 等级相同。危机解除后，进程自动降回 USER 等级。

等级标记沿固定路径在系统中传递：进程通过 fork 创建时继承父进程等级，通过 exec 加载 ELF 文件时由 elf_assign_tier 函数重新分配等级，调度器按等级将任务入队并令高等级优先运行，内存管理按等级分配物理页，I/O 子系统按等级将请求入队，沙盒机制按等级限制进程能力，进程退出时标记为 ZOMBIE 并由父进程回收。

等级与传统优先级的本质区别体现在三个维度。第一，传统优先级仅影响 CPU 时间片，而等级贯穿 CPU、内存、I/O 和安全四个维度。第二，传统优先级是数值，用户可通过 nice 命令随意修改，而等级有明确的系统级语义，分配由系统决定，用户无法随意提升。第三，传统优先级无自我保护机制，而 KERNEL 等级受绝对保护，CRITICAL 等级可自动触发。
3 调度器设计

HTSOS 使用五级队列管理就绪任务。队列零为 KERNEL 等级，队列一为 SYSTEM 等级，队列二至四为 USER 等级的三个子队列，分别对应交互型、混合型和 CPU 密集型任务。

USER 队列内部采用多级反馈队列算法进行动态调整。进程提前让出 CPU（使用时间片少于分配量的百分之五十）时被视为交互型任务，提升一级；进程耗尽完整时间片时被视为 CPU 密集型任务，降一级；进程超过两百毫秒未获得调度时，系统将其全局提升一级以防止饥饿。该设计使交互型任务自动获得高优先级，使 CPU 密集型任务自动降级。

各等级时间片如下：KERNEL 等级五毫秒，SYSTEM 等级十五毫秒，USER_L0 等级二十毫秒，USER_L1 等级三十毫秒，USER_L2 等级五十毫秒。

抢占规则遵循严格的等级秩序。KERNEL 可抢占任何任务，SYSTEM 可抢占任意 USER 子队列任务，USER_L0 可抢占 USER_L1 和 USER_L2，USER_L1 可抢占 USER_L2。同等级队列内部采用先来先服务策略。

系统开销单独记账是 HTSOS 调度器的重要设计。中断处理和内核线程的开销不计入任何用户进程的时间片，解决了传统系统中高等级进程因硬件中断频繁而被无辜扣减时间片的问题。
4 内存管理

HTSOS 的内存管理遵循按等级分配的策略。KERNEL 等级永远优先分配内存，拥有预留物理页池，分配永不失败。SYSTEM 等级分配优先级高，在内存压力时优先保证。USER 等级正常分配，在压力时受限。CRITICAL 等级的分配策略与 SYSTEM 等级相同。

内存紧张时，系统按固定顺序驱逐物理页：首先驱逐 USER_L2 等级的页，然后依次为 USER_L1、USER_L0、SYSTEM。KERNEL 等级的页永不驱逐。

压力触发 CRITICAL 机制是 HTSOS 内存管理的核心功能。当 alloc_page_frame 函数分配物理页失败时，系统调用 vm_handle_pressure 函数。若当前进程为 USER 等级且空闲页数量低于临界阈值，该进程被提升为 CRITICAL 等级，系统输出压力触发日志。当空闲页数量恢复后，CRITICAL 等级进程降回 USER 等级，系统输出压力释放日志。
5 I/O 队列分级

HTSOS 在块设备抽象层实现三级 I/O 队列。KERNEL、SYSTEM 和 CRITICAL 等级的 I/O 请求进入金级队列，采用抢占式策略，插入队头并立即处理。USER_L0 和 USER_L1 等级的 I/O 请求进入银级队列，采用加权公平队列算法，权重比为二比一。USER_L2 等级的 I/O 请求进入铜级队列，采用批量合并策略，攒满六十四千字节或一百毫秒超时后提交。

该设计确保系统关键 I/O（如文件系统日志、看门狗）不受低等级 I/O 阻塞，同时通过批量合并降低中断频率，减少 CPU 开销。
6 沙盒机制

HTSOS 通过 fork_with_sandbox 系统调用提供沙盒能力。该系统调用接受沙盒配置结构体作为参数，包含四个限制维度：能力位掩码、内存上限、CPU 配额和根路径。

HTSOS 定义了十二种细粒度能力位，包括读文件、写文件、执行程序、挂载文件系统、杀死进程、重启系统、关机、网络操作、修改等级、进程间通信、改变根目录、内存映射和创建沙盒。

不同等级拥有不同的默认能力集。KERNEL 等级拥有全部能力。SYSTEM 等级拥有读写执行、挂载、杀进程、重启、网络、修改等级和进程间通信能力。USER 等级仅拥有读写执行和进程间通信能力。CRITICAL 等级的能力与其原等级相同。

等级只能降不能升是 HTSOS 沙盒的信任边界。SYSTEM 进程可以创建 USER 子进程，但 USER 进程不能创建 SYSTEM 子进程。KERNEL 不由用户态进程创建。CRITICAL 由系统自动授予，不由用户态请求。
7 锁与优先级继承

HTSOS 提供两种锁类型：自旋锁用于短暂临界区，通过原子操作实现；互斥锁支持阻塞等待，包含等待队列。

优先级继承机制解决了优先级反转问题。当高等级进程等待低等级进程持有的锁时，低等级进程临时提升至高等级，释放锁后恢复原等级。该机制防止高等级进程被低等级进程长时间阻塞。

全局锁顺序定义如下：调度器锁优先于进程表锁，进程表锁优先于内存管理锁，内存管理锁优先于 VFS 锁，VFS 锁优先于设备驱动锁。所有代码必须按此顺序获取多个锁，按相反顺序释放，以防止死锁。
8 看门狗与自愈

看门狗线程以 SYSTEM 等级运行，定期检查所有 SYSTEM 等级进程的存活状态。检测到进程崩溃时，系统记录崩溃次数，按照指数退避策略延迟一秒、两秒、四秒、八秒、九秒后重新加载该进程。若同一进程连续崩溃达五次，系统停止重启尝试并输出报警信息。

该机制确保关键系统进程在崩溃后自动恢复，无需人工干预，提升了系统的可用性。
9 多核准备

HTSOS 当前实现为单核调度器，但已预留多核扩展接口。每个 CPU 核心拥有独立的等级队列，调度器从当前 CPU 的队列中选取任务。负载均衡框架以预留接口形式存在，具体迁移逻辑尚未实现。CPU 亲和性接口已定义，可供未来实现。
10 已实现功能与性能模型

截至本文档撰写时，HTSOS 已实现以下功能：四级等级体系、五级 MLFQ 调度、等级时间片分配、高等级抢占、按级内存分配、CRITICAL 压力触发、沙盒辅助 fork、看门狗自愈、系统调用能力门控、I/O 三级队列、自旋锁与互斥锁、优先级继承以及 per-CPU 调度队列接口。

HTSOS 采用集成设计，等级信息随任务结构体自然传递，在各子系统间直接共享。与模块化设计相比，等级查询从四次独立读取减少为一次统一读取，内存分配从查表加分支优化为直接索引，上下文切换从三至五次状态保存减少为两次状态保存，跨模块协作从接口消息传递优化为直接共享。

在等级感知路径上，集成设计的性能预期为模块化设计的二至三倍。
11 总结

HTSOS 是一个等级感知、全栈贯穿的调度框架。它通过等级契约替代公平调度，通过 CRITICAL 机制保护内核自身，通过看门狗实现系统自愈，通过沙盒辅助 fork 弥补传统安全缺陷，通过集成设计换取性能。

HTSOS 的设计方向与主流调度器不同，其核心价值在于内核自我保护、安全与调度的融合以及代码的可理解性。它是一个完整的调度框架，也是一套经过工程验证的设计哲学。
