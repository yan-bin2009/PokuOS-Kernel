HTSOS 调度框架技术文档
版本 2.3

一、概述

HTSOS 是一个等级感知的调度框架。它将进程按重要性划分为四个等级，等级标记贯穿调度器、内存管理、文件系统和 I/O 子系统。所有资源分配决策以等级为第一优先级依据。

核心设计目标：确保 KERNEL 等级进程在任何负载下不被驱逐，SYSTEM 等级进程在内存压力下优先存活，USER 等级进程在资源充足时公平运行，CRITICAL 等级在检测到内存压力时自动激活。

设计前提：系统存在关键路径进程（init、shell、watchdog），这些进程的存活优先级高于用户进程。内存耗尽时系统应优先终止用户进程而非关键进程。

二、等级定义

四个等级使用枚举类型 tier_t 表示：

TIER_KERNEL = 0
TIER_SYSTEM = 1
TIER_USER = 2
TIER_CRITICAL = 3

调度优先级与等级数值成反比，数值越小优先级越高。

KERNEL 进程：调度器硬抢占任何低等级进程。内存分配永不失败（预留物理页池）。不参与驱逐。

SYSTEM 进程：可抢占 USER 等级进程。内存分配在压力时优先于 USER。仅在 KERNEL 需求之后参与驱逐。

USER 进程：正常调度。内存分配受限制。驱逐顺序中排首位。

CRITICAL 进程：调度和内存策略等同于 SYSTEM。仅在内存压力触发时由 USER 提升而来，压力解除后降回 USER。

三、调度器

3.1 队列结构

五个调度队列，按优先级降序排列：

queue[0] = KERNEL
queue[1] = SYSTEM
queue[2] = USER_L0
queue[3] = USER_L1
queue[4] = USER_L2

每个队列为双向链表，存储 task_t 结构体指针。

3.2 MLFQ 升降级规则

USER 等级内部包含三级子队列（L0、L1、L2）。进程初始入队位置由 elf_assign_tier() 返回的等级决定，USER 等级默认进入 L1。

升降级条件：

    进程在时间片内主动让出 CPU（调用 yield 或阻塞于 I/O），且实际运行时间小于时间片的 50%：提升一级（L2→L1，L1→L0）

    进程耗尽完整时间片：降一级（L0→L1，L1→L2）

    进程连续 200ms 未被调度：强制提升一级（防饥饿）

3.3 时间片

时间片单位为毫秒：

KERNEL: 5ms
SYSTEM: 15ms
USER_L0: 20ms
USER_L1: 30ms
USER_L2: 50ms

时间片在进程入队时设置，每次时钟中断递减。归零时触发重新调度和等级调整。

3.4 抢占规则

时钟中断处理程序在每次中断时执行：

    检查当前运行进程等级

    检查各非空队列中是否存在等级更高的进程

    若存在，设置 need_resched 标志，中断返回前调用 schedule()

具体抢占条件：
if (current->tier > 最高非空队列的等级) 触发抢占

同等级内不抢占，按 FIFO 轮转。

3.5 调度入口

schedule() 函数执行：

    从最高等级非空队列取出第一个进程

    若该进程与 current 不同，调用 switch_to() 切换上下文

    若所有队列为空，执行 idle 任务

四、内存管理

4.1 按级分配

alloc_page_frame(tier_t tier) 根据等级选择物理页来源：

    KERNEL：从预留池分配，预留池大小在 paging_init 时配置（默认总内存的 1%）

    SYSTEM：从全局空闲链表分配，若分配失败则触发驱逐 USER 页

    USER：从全局空闲链表分配，若分配失败则触发压力检测

    CRITICAL：同 SYSTEM

4.2 驱逐顺序

内存回收时按以下顺序选择 victim 页：

    USER_L2 页

    USER_L1 页

    USER_L0 页

    SYSTEM 页

    KERNEL 页（永不回收）

每页在 vm_page 结构体中记录等级标记，驱逐时遍历物理页数组按等级排序选择。

4.3 CRITICAL 触发条件

vm_handle_pressure() 在 alloc_page_frame() 返回 NULL 时被调用：

    读取当前进程等级

    若等级为 USER，读取空闲页计数 free_pages

    若 free_pages < FREE_PAGES_CRITICAL（默认 64），将当前进程等级提升为 CRITICAL

    若 free_pages < FREE_PAGES_WARN（默认 128），输出警告日志

vm_release_pressure() 在 free_page_frame() 释放页后调用：

    若当前进程等级为 CRITICAL 且 free_pages > FREE_PAGES_CRITICAL * 2

    降级为 USER

五、沙盒辅助 fork

5.1 系统调用接口

int fork_with_sandbox(struct fork_sandbox_config *config)

struct fork_sandbox_config 定义：

    uint32_t caps_allow：允许的能力位掩码

    uint64_t mem_limit：内存上限（字节），0 表示不限制

    uint32_t cpu_quota：CPU 配额（百分比），0 表示不限制

    char *root_path：chroot 路径，NULL 表示不隔离

    int tier_override：覆盖等级，0 表示继承父进程

5.2 能力位掩码

能力位定义（32 位掩码）：

位 0：CAP_READ
位 1：CAP_WRITE
位 2：CAP_EXEC
位 3：CAP_MOUNT
位 4：CAP_KILL
位 5：CAP_REBOOT
位 6：CAP_POWEROFF
位 7：CAP_NET
位 8：CAP_SET_TIER
位 9：CAP_IPC
位 10：CAP_CHROOT
位 11：CAP_MMAP
位 12：CAP_SANDBOX
位 13-31：保留

默认能力集：
KERNEL：全部位（0xFFFFFFFF）
SYSTEM：位 0-9（0x000003FF）
USER：位 0、1、2、9（0x00000207）
CRITICAL：同原等级

5.3 等级检查

fork_with_sandbox 入口检查：
if (config->tier_override != 0 && config->tier_override < current->tier)
return -EPERM

等级数值越小优先级越高，因此检查“请求等级是否高于当前等级”等价于 config->tier_override < current->tier。

六、自愈机制

6.1 看门狗线程

watchdog 作为 SYSTEM 等级内核线程运行，循环执行：

    遍历 task 数组，检查所有 SYSTEM 等级进程的 state 字段

    若发现 state == TASK_ZOMBIE，记录崩溃次数

    计算延迟：delay = min(1 << (retry_count - 1), 9) 秒

    延迟后调用 exec 重新加载该进程的原始程序

    若 retry_count >= 5，停止重启并输出报警

6.2 进程状态

task_t.state 取值：

    TASK_RUNNING：可调度

    TASK_WAITING：阻塞等待（如 wait 系统调用）

    TASK_ZOMBIE：已退出，等待父进程回收

七、信任与安全

7.1 等级传递规则

fork()：子进程继承父进程等级。
exec()：调用 elf_assign_tier(path) 重新分配等级。
fork_with_sandbox()：若 config->tier_override 非零且合法，覆盖子进程等级。

7.2 提权防护

等级只能降不能升。系统调用入口执行检查：

    SYS_FORK：无等级参数，直接继承

    SYS_SANDBOX：检查 tier_override 不高于调用者等级

    SYS_TIER_REQUEST：仅允许请求 TIER_CRITICAL，且需内核判定压力状态

7.3 CRITICAL 防护

CRITICAL 等级不由用户态请求，由内核压力检测自动授予。同一时间最多允许 CRITICAL_MAX（默认 3）个 CRITICAL 进程。

7.4 沙盒路径隔离

VFS 层路径解析函数 vfs_lookup() 检查当前进程的 root_path：

    若 root_path 非空，解析结果路径必须位于 root_path 目录树下

    若路径解析结果在 root_path 之外，返回 ENOENT

此检查在每次路径解析时执行，不依赖 chroot。

八、设计取舍

集成设计：等级信息随 task_t 结构体传递，各子系统直接读取 current->tier。不经过消息或接口封装。代价是子系统间耦合度高，替换困难。

等级隔离优先：系统在 CPU 和内存资源紧张时优先保证高等级进程。代价是低等级进程可能饥饿。

半模块化：核心组件（等级、调度、内存、进程、沙盒）静态链接。外围组件（文件系统、驱动）支持模块加载。

九、已知技术缺陷

9.1 调度器

防饥饿周期固定 200ms，不随系统负载变化。负载低时响应偏慢，负载高时可能震荡。

优先级继承未实现。SYSTEM 进程等待 USER 进程持有的锁时，USER 进程不临时提升，导致 SYSTEM 进程被阻塞。

系统调用路径执行期间不提升等级。长时间系统调用（如 exec 加载大文件）以 USER 等级运行，可能被抢占。

9.2 内存管理

串口输出函数 serial_write 可能调用 kmalloc。内存耗尽时 vm_handle_pressure 调用 serial_write 打印日志，可能因 kmalloc 失败而无法输出，甚至递归死锁。

驱逐算法遍历物理页数组选择 victim，时间复杂度 O(N)，N 为总物理页数。内存较大时驱逐开销线性增长。

9.3 自愈机制

watchdog 仅检测进程退出，不检测内核子系统卡死。调度器死锁或内存管理死循环时 watchdog 无响应。

ZOMBIE 进程仅在父进程调用 wait 时回收。父进程不调用 wait 时 ZOMBIE 堆积，占用 task 数组槽位。

9.4 沙盒

路径白名单检查在 VFS 层实现，但文件描述符继承未清理。父进程预先打开的文件句柄在 fork 后仍可访问。

proc 文件系统未过滤沙盒进程视图。沙盒内进程可通过 /proc/1/mem 读取其他进程内存。

9.5 多核支持

调度队列为全局单队列，未实现 per-CPU 队列。多核环境下队列访问需加锁，成为竞争热点。

负载均衡未实现。高等级进程无法迁移到空闲核心。

9.6 可观测性

无性能计数器。调度延迟、上下文切换时间、内存分配延迟无测量数据。

无运行时状态查询接口。无法在线查看队列长度、各等级 CPU 占比、进程 MLFQ 等级。

9.7 配置

阈值硬编码于头文件：

    FREE_PAGES_CRITICAL = 64

    FREE_PAGES_WARN = 128

    MLFQ_UPGRADE_PERIOD = 200

    WATCHDOG_MAX_RETRIES = 5

    CRITICAL_MAX = 3

不同内存容量硬件需重新编译内核调整阈值。

9.8 安全

ELF 等级分配依赖路径名匹配。恶意程序命名为 /bin/shell 可获得 SYSTEM 等级。无签名验证或 inode 标记。

CAP_ALL（全能力位）作为单一权限值存在，削弱了能力模型的细粒度控制。

十、当前阶段优先修复

版本 2.3 当前阶段（M3）优先处理以下三项：

第一，serial_write 改用预分配环形缓冲区，移除 kmalloc 依赖。在 paging_init 时分配固定 4KB 缓冲区，写入指针循环使用。

第二，关键阈值改为启动参数。kernel_main 解析 cmdline 字符串，覆盖默认值。参数格式：poku.free_pages_critical=128 poku.mlfq_period=100

第三，实现 /proc/tier 接口。读取时输出所有进程的 pid、等级、MLFQ 子队列、运行时间。输出格式为纯文本表格。

十一、总结

HTSOS 是一个等级感知调度框架。四个等级（KERNEL、SYSTEM、USER、CRITICAL）贯穿调度、内存、I/O 和安全子系统。等级决定调度优先级、内存分配优先级、驱逐顺序和默认能力集。

已知缺陷集中在配置硬编码、多核缺失、可观测性不足、沙盒逃逸风险四个领域。修复路径已明确，按 M3、阶段五、阶段六分期实施。


目前在PokuOS上实现的是不完整版，属于实验版
