HTSOS 调度框架设计文档

一、概述

HTSOS（Hierarchical Tiered Scheduling Operating System）是一个等级感知、全栈贯穿、自愈优先的操作系统调度框架。它将进程按重要性划分为多个等级，并让这个等级标记贯穿调度、内存管理、文件系统、I/O 和安全沙盒等所有子系统。

HTSOS 的核心设计哲学是：内核永不宕机，关键任务有保障，普通任务公平，系统能自愈。

传统操作系统调度器追求的是"公平"，但公平并不总是最优的。系统关键进程应该优先于用户编译任务，内存紧张时应该保护内核自身不被 OOM 杀死。HTSOS 通过等级体系在系统中建立明确的优先级契约，确保关键路径在任何极端负载下都能存活。

二、等级体系

HTSOS 定义了四个等级，每个等级有明确的身份、调度优先级和内存驱逐策略：

KERNEL 等级是内核自身，拥有最高调度优先级，可以硬抢占任何任务，内存永不驱逐，负责运行内核线程和中断处理。

SYSTEM 等级是系统关键进程，拥有高调度优先级，可以抢占 USER 等级的任务，内存驱逐优先级极低，典型进程包括 init、shell 和 watchdog。

USER 等级是普通用户进程，调度优先级正常，内存可以被驱逐，运行用户程序和第三方程序。

CRITICAL 等级是 USER 的临时提升状态，当系统内存压力增大时，当前正在运行的 USER 进程会被自动提升到这个等级。它的调度优先级和内存驱逐策略与 SYSTEM 相同，危机解除后自动降回 USER。

等级标记在系统中沿着固定的路径传递：进程通过 fork 创建时继承父进程的等级，通过 exec 加载 ELF 文件时由 elf_assign_tier 函数重新分配等级，调度器按等级将任务入队并让高等级优先运行，内存管理按等级分配物理页，沙盒机制按等级限制进程能力，进程退出时标记为 ZOMBIE 并由父进程回收。

等级与传统优先级的核心区别在于：传统优先级仅影响 CPU 时间片，而 HTSOS 的等级贯穿 CPU、内存、I/O 和安全四个维度；传统优先级是数值，可以被用户随意修改，而 HTSOS 的等级有明确的系统级语义，等级分配由系统决定，用户无法随意提升；传统优先级没有自我保护机制，而 HTSOS 的 KERNEL 等级受到绝对保护，CRITICAL 等级可以自动触发。

三、调度器设计

HTSOS 使用五级队列管理就绪任务。队列 0 是 KERNEL 等级，拥有最高优先级，可以硬抢占任何任务。队列 1 是 SYSTEM 等级，拥有高优先级，可以抢占 USER 等级的任务。队列 2、3、4 是 USER 等级的三个子队列，分别对应交互型、混合型和 CPU 密集型任务。

USER 队列内部使用多级反馈队列算法进行动态调整。当进程提前让出 CPU（使用的时间片少于分配时间片的 50%）时，被视为交互型任务，提升一级。当进程耗尽完整时间片时，被视为 CPU 密集型任务，降一级。如果进程超过 200 毫秒未获得调度，系统会将其全局提升一级以防止饥饿。

这种设计让交互型任务（如频繁进行 I/O 操作、等待用户输入的程序）自动获得高优先级，让 CPU 密集型任务（如编译、计算）自动降级，让出 CPU 资源。

不同等级拥有不同的时间片长度。KERNEL 等级为 5 毫秒，实现极速响应。SYSTEM 等级为 15 毫秒，实现均衡调度。USER_L0 为 20 毫秒，优先保障交互体验。USER_L1 为 30 毫秒，作为默认配置。USER_L2 为 50 毫秒，优先保障吞吐量。

抢占规则遵循严格的等级秩序：KERNEL 可以抢占任何任务，SYSTEM 可以抢占任意 USER 子队列的任务，USER_L0 可以抢占 USER_L1 和 USER_L2，USER_L1 可以抢占 USER_L2。同等级队列内部采用先来先服务策略。

系统开销单独记账是 HTSOS 的一个重要设计。中断处理、内核线程的开销不计入任何用户进程的时间片，解决了传统系统中进程因硬件中断而无辜被扣时间片的问题，确保高等级进程不会因为网络数据包到达频繁而被降权。

四、内存管理

HTSOS 的内存管理遵循按等级分配的策略。KERNEL 等级永远优先分配内存，拥有预留池，永不失败。SYSTEM 等级拥有高分配优先级，在内存压力时优先保证。USER 等级正常分配，但在内存压力时受限。CRITICAL 等级的分配策略与 SYSTEM 相同。

当内存紧张时，系统按固定的顺序驱逐内存页：首先驱逐 USER_L2 等级的页，然后是 USER_L1，接着是 USER_L0，之后是 SYSTEM，而 KERNEL 等级的页永不驱逐。

压力触发 CRITICAL 机制是 HTSOS 内存管理的核心功能。当 alloc_page_frame 函数分配物理页失败时，系统调用 vm_handle_pressure 函数。如果当前进程是 USER 等级且空闲页数量低于临界阈值，该进程被提升为 CRITICAL 等级，系统输出压力触发日志。当空闲页数量恢复后，CRITICAL 等级进程降回 USER 等级，系统输出压力释放日志。

五、沙盒辅助 fork

传统的 fork 系统调用存在安全缺陷：子进程继承父进程的所有能力和资源，可能滥用这些权限，而且需要在 fork 之后手动降权，存在安全窗口。

HTSOS 通过 fork_with_sandbox 系统调用解决了这个问题。这个系统调用接受一个沙盒配置结构体作为参数，其中包含了允许的能力位掩码、内存上限、CPU 配额、chroot 路径和等级覆盖选项。

HTSOS 定义了 15 种细粒度的能力权限位，包括读取文件、写入文件、执行程序、挂载文件系统、杀死其他进程、重启系统、关机、网络操作、修改等级、进程间通信、改变根目录、内存映射和创建沙盒等。

不同等级拥有不同的默认能力集。KERNEL 等级拥有全部能力。SYSTEM 等级拥有读写执行文件、挂载、杀进程、重启关机、网络、修改等级和进程间通信的能力。USER 等级仅拥有读写执行文件和进程间通信的能力。CRITICAL 等级的能力与其原等级相同。

六、自愈机制

看门狗是 HTSOS 的自愈核心。看门狗线程定期检查所有 SYSTEM 等级进程是否存活。当检测到进程崩溃时，系统记录崩溃次数，按照指数退避策略延迟 1 秒、2 秒、4 秒、8 秒、9 秒后重新加载该进程。如果同一进程连续崩溃达到 5 次，系统停止重启尝试并打印报警信息。

自愈机制的日志输出清晰记录了每次崩溃和重启事件，便于运维人员了解系统状态。

七、设计哲学

HTSOS 遵循三个核心设计原则。

机制与策略分离：内核提供稳定的执行底座，策略在顶层可配置。这保证了内核的稳定性和灵活性。

等级贯穿全栈：tier 从调度到内存到 I/O 到沙盒，贯穿整个系统。这是 HTSOS 区别于传统调度器的根本特征。

渐进式实现：每阶段有明确验证标准，不搞一次性大爆炸。这保证了系统的可控演进。

在性能方面，HTSOS 采用集成设计。等级信息随 task_t 结构体自然传递，在各子系统间直接共享，不需要通过消息或接口传递。等级查询只需一次统一读取，内存分配直接索引而不需要额外查表，上下文切换只需两次状态保存，跨模块协作直接共享状态而不需要封装开销。

八、总结

HTSOS 是一个等级感知、全栈贯穿、自愈优先的操作系统调度框架。它的核心价值在于：用等级契约替代公平调度，用 CRITICAL 机制保护内核自身，用看门狗实现系统自愈，用沙盒辅助 fork 弥补传统安全缺陷。

HTSOS探索一种不同的设计方向——在内核自我保护、安全与调度融合、代码可理解性上走得更远。它是一个约一万行代码、有原创思想的自研操作系统的核心调度框架。

注意，目前你看到的代码是HOTSOS不完整版，不是一个真正的版本

